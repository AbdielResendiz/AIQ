import React from 'react';
import { View, Text } from 'react-native';

const DetallePedido = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Estás viendo el detalle del pedido</Text>
    </View>
  );
};

export default DetallePedido;
